[build-system]
requires = ["maturin>=1.7,<2.0"]
build-backend = "maturin"

[project]
name = "peerbus"
version = "0.3.1"
description = "Python bindings for the peerbus messaging library"
requires-python = ">=3.11"

[tool.maturin]
features = ["python"]
module-name = "peerbus"
profile = "python"

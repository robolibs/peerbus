[build-system]
requires = ["maturin>=1.7,<2.0"]
build-backend = "maturin"

[project]
name = "quicbit"
version = "0.2.0"
description = "Python bindings for the quicbit messaging library"
requires-python = ">=3.9"

[tool.maturin]
features = ["python"]
module-name = "quicbit"
